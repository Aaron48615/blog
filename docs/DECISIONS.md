# 决策日志

已定决策，后来者必须遵守。要推翻：新增一条"提议"条目并等人确认，不许直接改代码。
格式：`日期 | 决策 | 理由 | 定下者`

- 2026-08-25 | 仓库结构：monorepo + pnpm workspace | 记忆文档只维护一份，多 AI 协作摩擦最小 | 用户 + OpenCode
- 2026-08-25 | 部署平台：Vercel，每个 app 一个 project | 免费、push 即部署、monorepo Root Directory 原生支持 | 用户
- 2026-08-25 | 记忆体系：AGENTS.md 为唯一源头，CLAUDE.md 仅一行 `@AGENTS.md` 导入 | 避免双份内容漂移 | 用户 + OpenCode
- 2026-08-25 | 分工：Codex=逻辑/结构/部署，Antigravity=视觉，OpenCode Go=内容/杂活，Claude Code=review | 按层切分，同一时间一个文件只有一个 AI 碰 | 用户 + OpenCode
- 2026-08-25 | 所有产物纯静态构建，仓库内不含服务端 | 两个旧项目的后端均为第三方 API | 用户
- 2026-08-25 | apps/site 用 Astro | 内容型个人站首选，默认零 JS | 用户确认
- 2026-08-26 | 旧项目源目录：project-1 来源 `/Users/aaron/LoveCoding/21_React/高阶/data-pilot`，project-2 来源 `/Users/aaron/LoveCoding/20_Vue3/mobile-shop` | 用户已提供，作为迁移来源 | 用户
- 2026-08-26 | project-1 采用 Vercel rewrites：`/api/:path*` 转发至 `http://116.62.230.90/api/:path*`，SPA 路由回退至 `/index.html` | 实测 `:9999` 提供 API 文档且业务路径返回 404；端口 80 的 `/api/auth/captcha` 返回正常 JSON，并已在 Vercel 预览中验证 | 用户 + Codex
- 2026-08-27 | project-2 按用户本轮明确要求采用 Vercel Serverless Function 转发商城 `/api/*` 至 `http://shop-api.edu.koobietech.com/*`；作为“仓库不含服务端”的限定例外，仅增加代理，前端仍静态构建，商城业务后端仍为第三方 | 用户明确指定 Serverless 代理方案；不影响 project-1 与 site 的既有方案 | 用户
- 2026-08-27 | 提议（待确认）：本轮不向 `VITE_AI_API_KEY` 写入真实共享密钥，AI 保留现有无配置兜底；共享 AI 服务改用不含 `VITE_` 前缀的服务端密钥及鉴权/限流代理另行实施 | 现有前端读取该变量，Vite 会将真实 key 打入前端产物，与本轮验收冲突 | Codex 提议
- 2026-08-27 | 用户已明确派发共享 AI 代理任务：确认前条服务端密钥方向，project-2 增加 Vercel `/api/ai`，仅服务端读取 `DEEPSEEK_API_KEY`；匿名调用按 IP 默认每分钟 10 次/每小时 50 次，保留本地兜底，不复用商城 Authorization | 限定扩展 Serverless 例外；本轮用实例内存滚动窗口实现基础保护，不承诺跨实例全局配额或费用硬上限；更强鉴权/共享存储另行决策 | 用户（任务范围）+ Codex（实现及边界说明）
- 2026-08-28 | 用户确认将 project-2 Production 的服务端 AI 配置纳入功能修复任务，允许核对并调整必要的服务端 AI 环境变量、使配置生效及验证线上调用 | 搜索页注释定位当前生产 `/api/ai` 返回 503 / `AI service is not configured`；此前成功联调仅覆盖 Preview。保留服务端密钥及现有限流方案，不把密钥写入源码、`VITE_*` 或聊天，不改路由/代理逻辑，不直接推 main | 用户明确回复“允许”
- 2026-08-28 | 用户通过首页新注释明确授权 Codex 调整首页底色与 body 两侧留白，作为本轮功能修复中“禁止改视觉”的限定例外；最终以用户后续更正为准：首页底色 `#F7F4F2`，两侧纯白 `#FFFFFF` | 仅调整 Home.vue 页面背景变量及 main.css 的 body 背景；保留白色卡片、导航栏、其他页面内部配色、布局与业务逻辑，不扩展为全站主题重做 | 用户明确页面注释及后续更正
- 2026-08-28 | 用户通过地址页注释明确授权 Codex 修复地址列表固定顶部栏未匹配手机容器宽度的问题 | 仅调整 Address.vue 地址列表顶部栏限宽与居中；保留 fixed 定位、配色、地址数据与增删改逻辑，不将修复推广到其他页面或新增/编辑地址栏 | 用户明确页面注释
- 2026-08-28 | 用户明确指定 Codex 执行 site 第二轮注释驱动的样式和布局微调，作为默认视觉分工的本轮例外；DESIGN.md v3 与既有审计仅作参考，具体调整以本轮注释为准 | 仅修改点名页面/组件，不改内容模型、路由、文章数据、导航/目录/名言轮播功能逻辑；涉及内容、功能或范围不清的注释先确认 | 用户明确任务授权
- 2026-08-28 | 用户明确将首页改为“首屏仅名言与底部悬浮倒三角，下滑后个人介绍从左、双按钮从右进入并左右排列”，替代 DESIGN.md v3 的首页单屏不可滚动要求 | 本轮先供用户看效果；保持原名言/介绍文本和链接，不改轮播机制。仅首页导航移到首屏之后，窄屏改上下排列；CSS 滚动动画在不支持的浏览器及减少动态效果模式下静态展示内容，不新增滚动监听或点击逻辑 | 用户明确口述首页效果；Codex 实现及适配边界
- 2026-08-28 | 更正前条实现解释：首页导航应始终固定在视口顶部；“首屏只有名言”描述的是主体内容，不代表移走全站导航 | Codex 将导航移到第二段是理解失误，现按用户纠正恢复首页 fixed 顶部导航；保留首屏名言/倒三角与下滑左右入场，不修改其他页面导航或链接逻辑 | 用户明确纠正；Codex 记录
- 2026-08-28 | 在首页介绍文案确认问题后，用户通过新注释明确要求“这几句都不用句号”，本轮仅删除三句末尾句号，并按注释将介绍右对齐、介绍与按钮靠细竖线两侧 | 保留当前措辞，不扩大为此前尚未确认的“一个计算机新人”替换；继续保留 Aaron 字号强调、纵排按钮和入场效果，窄屏上下布局以横线分隔 | 用户明确新注释；Codex 限定实现范围
- 2026-08-29 | project-1 生产 `/api/*` 从外部 Vercel rewrite 改为项目内 Vercel Serverless 代理，替代 2026-08-26 决策中的生产代理实现；仍固定转发至 `http://116.62.230.90/api/*`，前端 `/api` baseURL、登录字段与本地 Vite 代理不变 | `city.aaronsite.top` 静态页正常但浏览器验证码请求 10 秒超时，生产 curl 同样出现 12 秒零字节超时；同一上游连续 5 次仅约 0.16–0.20 秒。本轮用户明确允许按必要性最小调整 API 代理；Serverless handler 提供 8 秒受控超时、最小请求头转发、Cookie 隔离与可测试错误契约 | 用户（任务范围）+ Codex（证据与实现）
- 2026-08-29 | project-1 生产 `/api/*` 回滚为 2026-08-26 的 Vercel 外部 rewrite，撤销同日 Serverless 代理决策；前端 `/api` baseURL、登录字段与本地 Vite 代理仍不变 | 用户补充线上对照证据：旧部署 934c3a2 的 Deployment URL 可刷新验证码并完整登录，Serverless 版本在 main Branch URL 与自定义域名无法刷新并进入 404/失败路径；本轮明确要求优先恢复生产可用，接受外部 rewrite 可能存在的偶发超时风险，后续若复现再单独讨论 | 用户明确要求 + Codex 执行
- 2026-08-29 | 记忆体系扩展：根 `AGENTS.md` 继续作为所有 AI 的唯一入口和稳定总纲，高频踩坑迁入 `docs/GOTCHAS.md`；`docs/` 注册为独立 pnpm workspace，OpenCode、执行者和 reviewer 开工时均需读取 Gotchas 分册 | Vercel 会把 workspace 外的 `docs/*` 视为全局变化，导致三个 app 同时部署；将高频记忆归入无 app 依赖的 docs workspace 可保留统一记忆并减少无关部署，根级配置变化仍按全局变化处理 | 用户明确同意完整迁移 + Codex 实施
