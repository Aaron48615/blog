# project-2 商品对比验收记录

日期：2026-09-09。执行及自审：Codex。用户本轮明确授权自审通过后自行合并。

## 实现范围

- 首页、分类、搜索、详情四个加入入口；一次两件、去重、移除、清空。Pinia 只持久化商品与 SKU ID，刷新重新读取详情。
- 对比页沿用登录守卫、商城请求及图片代理；动态属性、只看差异、库存、商家说明、型号保护、SKU 选择、预算与需求输入。失效项可移除，失败可重试。
- SKU price 为原价，商品顶层 price 为现价。69 的1199原价不被列表0.01误伤；异常原价不交给模型作为优势。页面和服务端均由程序判断原价预算，模型只解释已知信息。
- 可收起浮栏，站内导航和继续加入保持收起，清空后重置；桌面入口对齐手机容器，不增加底部占位。详情轮播上方放返回和对比按钮，去选购重新校验并预选SKU。
- Vercel `/api/compare/advice` 从固定商城重新校验商品/SKU归属和库存；复用服务端DEEPSEEK配置及匿名IP限流规则。独立取消，55秒超时，平台60秒上限并启用supportsCancellation。
- 未整体覆盖现有文件。保留旧搜索AI/智能卖点、首屏加载、图片代理、骨架屏、桌面限宽、购买弹窗及业务写入流程。参考项目只读，未读取或复制其密钥文件。

## 自动化及构建

102项测试通过，覆盖模型/持久化、跨品类属性、SKU顺序、异常价格、SSE中文逐字节与合块、取消/断流、输入与Origin校验、可信商品数据、缺key、超时、上游失败、限流及独立并发，保留旧模块回归。

强制类型检查、生产构建、改动文件Prettier和diff检查通过。AI产物检查97个文件通过；此检查不代表扫描了未读取的真实密钥值。

Preview首轮暴露仅部署可见的模块缺失：生成JS仍引用.ts。新增JS-only ESM加载测试先红后绿，根tsconfig启用rewriteRelativeImportExtensions；相同Preview GET由500变405，POST真实流式成功。

## 浏览器验收

Playwright Chrome，本地生产构建。正常商品读取真实商城；异常和流竞争在独立浏览器临时拦截，不修改后端。

- 首页/分类/搜索加入不跳转；第三件不加入，移除/清空、清空后展开通过。收起后跨路由、加入第二件仍收起。
- 375/320/430/1280宽度下无横向溢出，桌面封顶375。等rem调整完成后根字号37.5/32/37.5/37.5，详情宽度375/320/375/375。收起后底部购买按钮可命中。
- 真实69 SKU395显示原价1199/现价0.01，1200预算内；切到SKU396并返回详情显示42.5规格。刷新保留ID并重新加载数据。
- 美妆/食品动态净含量、空属性默认规格、无库存禁用、异常原价、失效商品可移除与重试恢复通过。
- 模拟流验证成功、需求/预算变化清空旧分析、旧响应不可覆盖、停止、提前断开保留部分文本并标未完成。
- 对比页375截图已目检。详情部分历史SKU图片链接存在失效，未修改既有轮播或后端资源。

## 线上验收与边界

Preview真实两鞋分析返回200 text/event-stream，193网络块，首包约3.7秒、总约7.2秒，最终done。取消配置部署后，跨品类分析280个delta并done，客户端中止通过。复用已有配置，无新增待配置项。

跨品类真实回答曾出现预算概括自相矛盾，页面程序计算正确；已将服务端程序计算的budgetStatus作为模型依据，禁止模型自行重算。复验真实跨品类分析200并done，模型正确复述手机2000元超出1000预算、精萃水890元在预算内；零预算前端明确拒绝。生成文本仍属建议，应以客观表格与商家确认信息为准。

自审通过后按no-ff流程合并生产。体验入口：https://shop.aaronsite.top/compare（需登录并选择商品）。

未进行加购、下单或商品写操作；未验真机Safari。新接口和旧AI端点的内存计数各自独立，不构成跨实例费用硬上限；供应商实际取消计费不在本次可核验范围。
